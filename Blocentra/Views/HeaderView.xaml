<UserControl x:Class="Blocentra.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Blocentra.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:Blocentra.Behaviors"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="32"
             Background="#1E2227">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Blocentra"
           FontSize="14"
           Foreground="White"
           VerticalAlignment="Center"
           Margin="10,0,10,0"
           Height="18">
           <i:Interaction.Behaviors>
                <behaviors:WindowDragBehavior EnableDrag="True"/>
           </i:Interaction.Behaviors>
        </TextBlock>

        <Button Grid.Column="1"
        Content="−"
        Width="40"
        Height="32"
        Background="Transparent"
        Foreground="White"
        BorderThickness="0"
        FontSize="18"
        Command="{Binding MinimizeCommand}"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        Cursor="Hand"/>

        <Button Grid.Column="2"
        Content="▭"
        Width="40"
        Height="32"
        Background="Transparent"
        Foreground="White"
        BorderThickness="0"
        FontSize="14"
        Command="{Binding MaximizeCommand}"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        Cursor="Hand"/>

        <Button Grid.Column="3"
        Content="✕"
        Width="40"
        Height="32"
        Background="Transparent"
        Foreground="White"
        BorderThickness="0"
        FontSize="14"
        Command="{Binding CloseCommand}"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        Cursor="Hand"/>
    </Grid>
</UserControl>
